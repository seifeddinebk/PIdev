{% extends 'admin/Base.html.twig' %}
{% block title %}
<title>Commentaire</title>
{% endblock %}



{% block body %}
<div style="margin-left: 500px; margin-top:100px; margin-right:200px; padding:50px;"> 
    <h1>Commentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire.id }}</td>
            </tr>
            <tr>
                <th>IdUser</th>
                <td>{{ commentaire.idUser }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ commentaire.contenu }}</td>
            </tr>
            <tr>
                <th>DateCommentaire</th>
                <td>{{ commentaire.dateCommentaire ? commentaire.dateCommentaire|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-secondary" href="{{ path('app_commentaire_index') }}">Retour à la liste des commentaires</a>
    </br>
    <a class ="btn btn-warning" href="{{ path('app_commentaire_edit', {'id': commentaire.id}) }}">Modifier</a>
    </br>

    <a class ="btn btn-danger" padding="1px;" href ="{{ include('commentaire/_delete_form.html.twig')}} </a>
    </div>
{% endblock %}
